# Langfuse Platform Template

This folder contains a sanitized self-hosted Langfuse Compose template.

1. Copy `.env.example` to `.env`.
2. Replace every `change-me` value.
3. Start with `docker compose up -d`.

Do not commit `.env`, generated credentials, database volumes, or object storage data.
